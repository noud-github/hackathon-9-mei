pipeline {
    agent { docker { image 'ubuntu' } }
    stages {
        stage('build') {
			steps {
				sh 'php --version'
				sh 'pwd'
				sh 'ls -al config/jenkins/'
				sh 'config/jenkins/test.cmd'
            }
        }
    }
}
